from pathlib import Path

ROOT = Path(__file__).resolve().parent
DATA_PATH = ROOT / "data" / "properties.csv"
PROFILE_DIR = ROOT / "profiles"
OUTPUT_DIR = ROOT / "output"
DEFAULT_PROFILE = "polo"
SHORTLIST_LIMIT = 20
